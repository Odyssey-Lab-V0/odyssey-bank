apiVersion: apps/v1
kind: Deployment
metadata:
  name: onboarding-service
  namespace: banking
  labels:
    app: onboarding-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: onboarding-service
  template:
    metadata:
      labels:
        app: onboarding-service
    spec:
      containers:
        - name: onboarding-service
          image: ghcr.io/odyssey-lab-v0/onboarding-service:7e70dd6
          ports:
            - containerPort: 8083
          envFrom:
            - secretRef:
                name: banking-db-secret
            - configMapRef:
                name: onboarding-service-config
            - secretRef:
                name: aiven-kafka-spring-ssl
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8083
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 5
          startupProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8083
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 30
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8083
            periodSeconds: 15
          volumeMounts:
            - name: kafka-ssl
              mountPath: /etc/kafka-ssl
              readOnly: true
          resources:
            requests:
              cpu: 100m
              memory: 512Mi
            limits:
              cpu: 500m
              memory: 1Gi
      volumes:
        - name: kafka-ssl
          secret:
            secretName: aiven-kafka-ssl
